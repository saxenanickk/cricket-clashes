import React from "react";
import { Home } from "./screens/Home";

const App = () => {
  /** Add Store Provider if using redux or any other state management system */
  return <Home />;
};

export default App;
