import { View } from "react-native";
import React from "react";

const HrLine = () => <View style={styles.container} />;

const styles = {
  container: { width: "100%", backgroundColor: "#45474c", height: 1 }
};

export { HrLine };
