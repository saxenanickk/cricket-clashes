const styles = {
  container: (isFull) => ({
    width: isFull ? "100%" : 90,
    height: 40,
    backgroundColor: "#a0a4d5",
    borderRadius: 5,
    justifyContent: "center",
    alignItems: "center"
  })
};

export { styles };
